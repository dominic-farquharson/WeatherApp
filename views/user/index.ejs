<!DOCTYPE html>
  <!-- head partial file -->
  <% include ../partials/head.ejs %>

  <body>
    <!-- Header partial -->
    <% include ../partials/header.ejs %>
    <main>


    <div><h2>hello <%= username %></h2></div>
    <div id="displayFavorites">
      <h2>History</h2>
      <% favorites %>
      <% favorites.forEach( (favorites) => { %>

          <div><h1><span>Search:</span><%= favorites.searchQuery %></h1></div>
          <p>Coordinates:</p>
          <div><span>Latitude:</span><%= favorites.latitude %></div>
          <div><span>longitude:</span><%= favorites.longitude %></div>
          <hr />
      <%  }) %>
    </div>
  </main>
    <% include ../partials/footer.ejs %>

  </body>
